// Firebase has been removed from this application
// Authentication is now handled by the custom PostgreSQL-based system
// using server/auth.ts and the NewAuthProvider hook

// Mock exports for backward compatibility to prevent import errors
export const auth = null;
export const analytics = null;
export default null;